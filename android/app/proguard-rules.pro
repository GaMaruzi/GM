# Regras ProGuard/R8 customizadas para o build de release.
# Por enquanto, as regras padrão (`proguard-android-optimize.txt`) cobrem tudo.
# Adicione aqui regras específicas quando introduzir bibliotecas que precisam
# de keep rules (ex: Room, Gson, kotlinx-serialization).
